name: quran_sessions_rtc_stub
resolution: workspace
description: >
  No-op RTC wiring for production builds without Agora/LiveKit native SDKs.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  get_it: ^9.2.1
  quran_sessions:
    path: ../quran_sessions
  quran_sessions_rtc:
    path: ../quran_sessions_rtc

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
