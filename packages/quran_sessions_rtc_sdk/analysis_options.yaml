include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Temporarily parked LiveKit sources (no livekit_client dep).
    - parked/**
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
