# @angular/fire declares optional peers (platform-browser-dynamic, platform-server,
# firebase-tools). Auto-installing them pulls in the deprecated
# @angular/platform-browser-dynamic package. We already use standalone
# bootstrap via @angular/platform-browser; those peers are unused.
legacy-peer-deps=true
