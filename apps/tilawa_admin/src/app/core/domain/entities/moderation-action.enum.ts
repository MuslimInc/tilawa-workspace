export enum ApplicationModerationAction {
  Approve = 'approve',
  Reject = 'reject',
  Suspend = 'suspend',
  Revoke = 'revoke',
}

export enum TeacherProfileModerationAction {
  Activate = 'activate',
  Deactivate = 'deactivate',
}

export enum UserModerationAction {
  Suspend = 'suspend',
  Reactivate = 'reactivate',
}
