<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ SplashScreen icon canvas — matches Flutter logo box
         [AppColors.launchSplashLogoFrameSize]. Bitmap is the full 1152 px
         frame with mask-safe padding baked in. -->
    <dimen name="splash_logo_frame_size">288dp</dimen>
    <!-- Masked visible diameter (192/288 × frame). Matches launchSplashLogoSize. -->
    <dimen name="splash_logo_display_size">192dp</dimen>
</resources>
