import 'package:flutter/foundation.dart';

/// Brand footer colors for share captures (ARGB components).
@immutable
class ShareFooterColors {
  const ShareFooterColors({this.backgroundArgb, this.foregroundArgb});

  final int? backgroundArgb;
  final int? foregroundArgb;
}
